# TRAC — Artist Career OS

## Overview
An artist-portfolio and career-management platform. Artists showcase artwork/collections/events on a public portfolio page, manage a private dashboard (uploads, CV, sketches, notes, work-in-progress projects), and can mint "blockchain COA" (certificate of authenticity) records tracked through a collector-facing companion app. Visitors can request an artist's CV via a public form without an account.

## Project Structure
```
├── index.html      # Marketing landing page (public) — sign in/sign up modal lives here
├── app.html        # The actual artist app: public portfolio + private dashboard (single file, all CSS/JS inline)
├── collector.html  # Collector-facing app: view/manage owned CoAs, report resales
├── server.py       # Trivial static file server for local/Replit preview only — not a real backend
├── schema.sql       # Blockchain COA table definitions + RLS policies
└── supabase/
    ├── functions/           # Edge Functions (notify-artist, send-cv-email)
    ├── cv_requests_rls.sql  # RLS policies for the CV-request feature
    ├── security_fixes_2026-07-26.sql  # RLS policy fixes — run against the live project
    └── storage_cleanup.sql, storage_updates_only.sql  # one-off storage.objects migrations
```
`/dashboard` and clean `tracstudio.app/{username}` portfolio URLs are Netlify `_redirects` rewrites to `app.html`, both staging and production. Most DB/storage changes (RLS policies, new columns) are applied directly via `supabase db query --linked` against both the staging (`utlgnwxulsasydqwcjgc`) and production (`vhgsayaugbepugssyary`) projects — not all of them are captured as `.sql` files in this repo, so `git log` / the Recent Changes section below is the more complete record for anything not listed under `supabase/`.

## Technology Stack
- Vanilla HTML, CSS, JavaScript — no build step, no bundler, no package.json
- Supabase (Postgres + Row Level Security, Auth, Storage, Edge Functions) as the entire backend
- Supabase JS client (v2) loaded from jsDelivr CDN
- Deno-based Supabase Edge Functions (TypeScript) for server-side email sending

## Architecture
- **No custom backend**: every database/storage operation happens directly from the browser via the Supabase client, using the (intentionally public) anon key. Security relies entirely on RLS policies and Storage bucket policies, not on hiding the key.
- **Storage bucket policies** (path-ownership, file-type/size limits for `artwork-images`/`cv-files`) are configured directly in the Supabase Dashboard and are not version-controlled in this repo — check there directly, not here.
- **Edge Functions** hold the two secrets that must never reach the browser: `RESEND_API_KEY` and `SUPABASE_SERVICE_ROLE_KEY`.

## Recent Changes
- 2026-08-13/08-14 (local only, not yet pushed to staging/main, commits `b046152`..`d9cfb1d`): Profile Settings redesign to match a concept mockup, plus a read-only security investigation into blockchain minting.
  - **Profile Settings redesign**: grouped Identity/Bio/Contact/Professional Materials/Social Media into bordered dashboard-card-style sections with icon headers, added a `.profile-header` avatar/name/handle/link card above the form (the avatar doubles as the photo-upload control -- a `<label>` wrapping the file input -- replacing the old separate file input + preview box), rebuilt Portfolio Collections as a card grid with a proper section divider, removed the now-duplicate "Your Portfolio Link" field (superseded by the header's link chip) along with `copyPortfolioLink()` and its dead CSS, and fixed several mobile-only bugs found after the fact: `.profile-header` never got mobile styles (never added to the existing mobile media query), the "New Collection" button was overflowing next to the heading, and there was no bottom padding after the last collection card.
  - **Security investigation (read-only, no code changed)**: confirmed via local `.sql` files (not live-queried -- no authenticated Supabase CLI session in this environment) that `coa_ownership_history`'s INSERT policy only checks that the coa/artist pair exists, not that the requester is the *current* owner -- a past owner can still re-transfer a CoA they no longer own, both in `collector.html`'s `executeTransfer()` and at the RLS layer; the schema file's own comment already flags this as a deliberately deferred gap, not an oversight. Also confirmed the Blockchain Tools production gate (`app.html` ~line 4230) only hides the dashboard button (`advancedToolsGrid.style.display = 'none'`) -- `showSection('blockchain')`, `openBlockchainTools()`, and `executeMint()` have no environment check at all, so any signed-in artist can mint a real CoA in production via the browser console today. Both flagged as follow-up work before minting goes live; see next session.
- 2026-08-12 (staging+main in sync, commits `02450c1`..`f09fe19`): Landing page copy tweaks, font-loading flash (FOUT) elimination, and mobile layout fixes across the portfolio and dashboard.
  - **Landing page tagline swap**: replaced "The Operating System for Artists" with "Ownership, On Record" in `index.html`'s title/hero, and trimmed trailing clauses from the hero subhead and "Built for your process" section copy.
  - **Font-loading flash fix**: `index.html`, `app.html`, and `collector.html` were all loading Space Grotesk from Google Fonts with `display=swap`, causing a visible flash of fallback text swapping to the real font on every load. Self-hosted all 5 weights under `/fonts/`, added a metrics-matched fallback `@font-face` (`size-adjust`/`ascent-override`/`descent-override`/`line-gap-override` computed directly from the real font files via a hand-built WOFF2 parser, benchmarked against Windows Arial) so any swap stays visually invisible, and added `<link rel=preload>` for the above-the-fold weights (400/500/600). Tried `font-display: optional` briefly to eliminate the swap outright, but its ~100ms budget can be missed on CPU-bound loads (not just slow networks) and then never swaps in the real font at all — reverted to `swap`, relying on the metrics-matched fallback instead.
  - **Landing page layout shift**: hero photo had no `width`/`height`, so its container collapsed to ~0 height until the 166KB image downloaded, then the page jumped. Added the image's real pixel dimensions (1536×952) so the browser reserves the space up front.
  - **Dashboard reorder**: moved "Your Uploaded Works" above the Studio/Activity History/CV Requests row, so it's the first thing under the stats.
  - **Portfolio profile tab, mobile**: the photo+bio block never switched from its desktop side-by-side flex row to a stacked layout on mobile, despite dead CSS that suggested one was intended (selectors that never matched the actual DOM). Added a real `flex-direction: column` override, let the photo fill the column width instead of staying capped at desktop's 45%, centered the now full-width "Works" nav button, and removed a redundant nested `@media (max-width: 768px)` block left over from an earlier fix.
- 2026-07-31/08-01 (staging+main in sync, commits `9bbad15`..`3627804`): Long session covering a live pentest, a functional bug, and a full pass on the portfolio style system.
  - **Pentest fixes**: anon `cv_requests` inserts were failing (RLS SELECT policy blocked the RETURNING clause) — client now generates the id via `crypto.randomUUID()` and skips `.select()`; `cv-files` storage bucket was public (readable by anyone with the URL) — set private, added an owner-scoped signed-URL policy, and the artist's own "view current CV" link now uses a short-lived signed URL; fixed a `send-cv-email` race (two simultaneous approvals could both email) by making the pending→approved transition itself the atomic claim (`UPDATE ... WHERE status='pending' RETURNING`), with rollback-to-pending on every downstream failure; `notify-artist` was silently 404ing on every call because production's `cv_requests` table was missing the `notified_at` column the function required; dropped several redundant/overly-broad RLS policies on `artwork_collections`, `moodboard_items`, `wip_notes`, `wip_sketches`, and `storage.objects` that were OR'd together with correct ones, silently neutralizing them.
  - **Fixed artwork editing**: `editArtwork()` was called from the dashboard's Edit button but had been deleted in a much older commit that added the caller without the function — `showUploadForm()` already fully supported edit mode, just needed the lookup wired back up.
  - **Public portfolio rendering bugs**: a leftover `setInterval` (`cleanPortfolioDisplay`, every 2s forever) was force-hiding every artwork's title/year/medium/dimensions on *every* public portfolio — turned out that was actually correct behavior (metadata is meant to show in the click-to-view lightbox, not inline), so after briefly removing it, restored the hide as clean static CSS scoped to `#portfolioPage` only (dashboard and the lightbox popup untouched). Also fixed: a collection's own display style (carousel/masonry/stacked/featured-first) was computed but then discarded in favor of a generic grid; 3 of 11 style presets referenced a `collectionTitleLayout` value with no matching case and silently fell back to an identical generic block.
  - **Moved "Browse Portfolio Styles"** from Profile Settings into a header icon button on the portfolio page itself, so applying a preset no longer means bouncing between two tabs to see the result.
  - **Style Editor fixes**: floating header had zero gap between Dashboard/Works nav (container drops max-width, leaving nothing for `justify-content: space-between` to distribute); "Show Contact in Footer" only ever worked for 2 of 5 footer styles; Archival Dossier's last-image-per-collection was missing its divider line while every other image had one (a `:last-child` rule that only made sense for a single continuous list, not one grid per collection).
  - **Redesigned 5 style presets** (Experimental Immersive, Monumental Sculptural, Historical Layered, Colorful Vibrant, Storytelling Quilt): root cause was that each preset's grid container selector (e.g. `.portfolio-layout.immersive-stacked`) had the same CSS specificity as the generic `.artworks-grid.small/.medium/.large` size rules and lost the cascade tiebreak — so all five were silently rendering as a plain auto-fill grid regardless of their actual design. Bumped selector specificity, added `object-fit: cover` (with the specificity/`!important` needed to beat a couple of other global leftover rules) so images fill masonry cells instead of leaving dead space, and replaced two layouts' rigid exact-card-count `nth-child` patterns with a repeating one via `grid-auto-flow: dense`.
  - **Added per-artwork bento-grid card sizing** (Small/Wide/Tall/Large, new `artworks.grid_size` column, default `small` = unchanged behavior). First shipped as a dropdown in the upload form, then moved on user feedback to a hover overlay directly on each card on the portfolio page itself (owner-only, public visitors never see it) — clicking a size writes straight to Supabase and reloads the grid in place.
  - User's plan: testing in production for about a week before further changes.
- 2026-07-26: Security audit fixes — added HTML-escaping (`escapeHtml`) across all user-generated content rendered via `innerHTML` in both `index.html` and `collector.html` (previously exploitable stored XSS on public portfolio pages); removed an RLS policy bug (`... or true`) that exposed all `collector_accounts` rows; tightened `coa_ownership_history`/`royalty_notifications` insert policies to require a real artist/coa relationship instead of `with check (true)`; fixed `notify-artist` Edge Function to re-derive request data server-side instead of trusting the client payload, and added HTML-escaping to both Edge Functions' email templates. See `supabase/security_fixes_2026-07-26.sql` for the RLS migration.
- 2025-11-09: Initial project creation
