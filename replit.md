# TRAC — Artist Career OS

## Overview
An artist-portfolio and career-management platform. Artists showcase artwork/collections/events on a public portfolio page, manage a private dashboard (uploads, CV, sketches, notes, work-in-progress projects), and can mint "blockchain COA" (certificate of authenticity) records tracked through a collector-facing companion app. Visitors can request an artist's CV via a public form without an account.

## Project Structure
```
├── index.html      # Marketing landing page (public) — sign in/sign up modal lives here
├── app.html        # The actual artist app: public portfolio + private dashboard (single file, all CSS/JS inline)
├── collector.html  # Collector-facing app: view/manage owned CoAs, report resales
├── server.py       # Trivial static file server for local/Replit preview only — not a real backend
├── schema.sql       # Blockchain COA table definitions + RLS policies
└── supabase/
    ├── functions/           # Edge Functions (notify-artist, send-cv-email)
    ├── cv_requests_rls.sql  # RLS policies for the CV-request feature
    ├── security_fixes_2026-07-26.sql  # RLS policy fixes — run against the live project
    └── storage_cleanup.sql, storage_updates_only.sql  # one-off storage.objects migrations
```
`/dashboard` and clean `tracstudio.app/{username}` portfolio URLs are Netlify `_redirects` rewrites to `app.html`, both staging and production. Most DB/storage changes (RLS policies, new columns) are applied directly via `supabase db query --linked` against both the staging (`utlgnwxulsasydqwcjgc`) and production (`vhgsayaugbepugssyary`) projects — not all of them are captured as `.sql` files in this repo, so `git log` / the Recent Changes section below is the more complete record for anything not listed under `supabase/`.

## Technology Stack
- Vanilla HTML, CSS, JavaScript — no build step, no bundler, no package.json
- Supabase (Postgres + Row Level Security, Auth, Storage, Edge Functions) as the entire backend
- Supabase JS client (v2) loaded from jsDelivr CDN
- Deno-based Supabase Edge Functions (TypeScript) for server-side email sending

## Architecture
- **No custom backend**: every database/storage operation happens directly from the browser via the Supabase client, using the (intentionally public) anon key. Security relies entirely on RLS policies and Storage bucket policies, not on hiding the key.
- **Storage bucket policies** (path-ownership, file-type/size limits for `artwork-images`/`cv-files`) are configured directly in the Supabase Dashboard and are not version-controlled in this repo — check there directly, not here.
- **Edge Functions** hold the two secrets that must never reach the browser: `RESEND_API_KEY` and `SUPABASE_SERVICE_ROLE_KEY`.

## Recent Changes
- 2026-09-12 (portfolio load-time investigation and fix, staging then production, commit `d49d451`): user-reported "portfolio loading taking so long now." Diagnosis first, no fixing until root cause confirmed. Checked in priority order: the 5 Google Fonts added by the 2026-09-11 style overhaul (confirmed primary cause), `netlify.toml` (cleared - deploy-time file copy only, no runtime overhead, no caching-header changes), the JS the overhaul added (cleared - new render functions are all conditionally invoked per-layout or owner-only, nothing runs unconditionally on a public view), and the async Supabase data-fetch waterfall on portfolio load (cleared - pre-existing, untouched by the overhaul commit, confirmed via `git show` finding zero query-code changes in it).
  - **Root cause**: a single unconditional `<link rel="stylesheet">` in `app.html`'s `<head>` requesting all 5 new families/11 variants on every load - dashboard and every portfolio alike - regardless of whether the active layout used any of them, with no `preconnect` to either `fonts.googleapis.com` or `fonts.gstatic.com`. Measured cold: 142ms just to TTFB on the font CSS itself, before the second-origin handshake for the actual font files even starts. `git log -S` traced the exact line to commit `0bb03be` (2026-09-11 portfolio style overhaul), confirmed present on both staging and production.
  - **Fix**: `ensurePortfolioAccentFonts(profileData)` replaces the static link. Built an exact layout-to-font mapping by grepping every `font-family` rule inside each `.portfolio-layout.<name>` CSS block (not guessed) - most non-classic layouts need `IBM Plex Mono` only; `gallery-wall`/`field-notes`/`vitrine`/`broadsheet`/`scroll` each need exactly one more family; TRAC Classic and the 3 newly-wired orphaned layouts need none. Also checks the artist's own `header_font_family`/`body_font_family`/`footer_font_family`/`portfolio_font_family` choice independently, since the Style Editor lets any of these 5 be picked on any layout. Only injects `preconnect` + a stylesheet request for the families actually needed, the first time a load needs one - a load needing none makes zero requests to either Google Fonts origin. Wired into both branches of `loadPortfolio()` and the Style Editor's live layout-switch handler.
  - One self-caught correction during the investigation: initially claimed `micrahman`'s `gallery-spaced` layout needed none of the 5 fonts as a live example; the actual CSS grep showed it does hardcode `IBM Plex Mono` for one accent. Didn't let the wrong example stand once found.
  - Verified live on both environments post-fix, real accounts/layouts, network-request counts not impressions: staging - `grid-classic` and `historical-overlap` both 0 font requests, a layout temporarily switched to `vitrine` correctly requested exactly `IBM Plex Mono` + `Cormorant Garamond` (screenshotted, renders correctly), then reverted. Production - `chloeparles` (grid-classic) 0 requests, `micrahman` and `alexofldp` (both `gallery-spaced`) each exactly 1 request (`IBM Plex Mono` only, was 5 families/11 variants before), all three screenshotted showing no visual regression against the pre-fix baseline.
  - Not done, flagged as lower-priority/longer-term: self-hosting these 5 fonts as `woff2` under `/fonts/` (same pattern as the existing Space Grotesk base font) to drop the third-party origin entirely.

- 2026-09-12 ("Beta" badge added to TRAC's own app chrome, staging then production, commits `ad53912`/`d0d8eff`): platform has no real users yet, minting is fully wired but `feature_flags.minting_enabled` is still `false` pending a separate go-ahead, and real bugs are still being found/fixed post-deploy - added a small, non-dismissible "Beta" pill next to the TRAC wordmark in `index.html`'s nav (covers the landing page and the login/signup overlay, which renders on the same page), `app.html`'s three private dashboard-chrome headers (Dashboard/Profile Settings/Blockchain), and `collector.html`'s header (same duplicated `.gallery-header` classes as `app.html`'s dashboard). Deliberately not added to the portfolio header (`generateHeaderHTML`/`#portfolioPage`) - confirmed structurally separate from the three touched headers (no shared render path) and confirmed live against real production data (`tracstudio.app/micrahman` shows zero TRAC chrome, zero badge). Verified light/dark (`--tc-ink-soft`/`--tc-border` variables, same pattern already used by `app.html`) and mobile (390px) on both environments.

- 2026-09-11/12 (standing pentest retest with a real session, two real vulnerabilities found and fixed on both staging and production, commit `0da3b11` plus the git-exposure fix below): a prior pentest pass couldn't get an authenticated session (Turnstile blocked fresh automated signup); retested the previously-inconclusive items using the existing `test-artist-2026-08-27@tracstudio-staging.test` account.
  - **Confirmed vulnerable, fixed - mint rate-limit TOCTOU race**: `mint-coa`'s 20/24h per-artist limit was a plain `COUNT` against `blockchain_coas`, followed several seconds later (after a full on-chain confirmation) by the `INSERT` that actually counted toward it next time - no lock spanned the gap, so concurrent/staggered calls could all read the same stale count and all pass. Fixed with `reserve_mint_slot()`/`resolve_mint_reservation()` (new `mint_rate_limit_reservations` table + two Postgres functions, advisory-lock-scoped per artist, `supabase/mint_rate_limit_atomic_2026-09-12.sql`) - count-and-reserve now happens atomically in one transaction. `mint-coa` resolves each reservation to `completed`/`failed` on every exit path so a failed attempt never permanently costs a real slot, and fails closed (500, never proceeds to mint) if the RPC itself errors.
  - **Confirmed vulnerable, fixed - nonce collisions**: concurrent `mint-coa`/`finalize-transfer` calls each built a fresh `ethers.Wallet`/`Contract` per request, so concurrent calls independently queried the chain's pending nonce and could collide, failing with a spurious 502 unrelated to the rate limit. Both functions now build one `NonceManager`-wrapped contract once per warm isolate instead of once per request.
  - **Confirmed vulnerable, fixed - signup email enumeration**: signing up with an already-registered email returned `identities.length === 0` from GoTrue and the app surfaced that directly as "An account with this email already exists," defeating GoTrue's own anti-enumeration design. Both cases (real new signup, already-registered) now fall through to one identical generic message, mirroring the password-reset flow's existing indistinguishable-response pattern.
  - **Confirmed safe (no change needed)**: double-minting (unique constraint on `blockchain_coas.artwork_id` held under 4-way real concurrency, exactly 1 success), transfer-initiation races (same pattern, `coa_pending_transfers`), double-finalize (atomic conditional `UPDATE ... WHERE status = 'pending'` claim, same pattern already verified for `notify-artist`), certificate immutability (direct `UPDATE` against `blockchain_coas` as the minting artist: `200` with 0 rows affected, RLS correctly blocks it), stored XSS (adversarial strings in title/medium/dimensions/description/bio render as escaped literal text on a real rendered portfolio page, not just a code read).
  - **Flagged, not fixed by design**: a timing side-channel on `/auth/v1/recover` (real accounts take ~3.4s to process vs ~66ms for fake ones, even though the response body/status are identical) - fixing it means a new proxy Edge Function mirroring `secure-login`'s pattern, judged non-trivial enough to not block the two real fixes above on it.
  - **Live boundary re-test, staging**: brought the account to 18/20 completed mints (one sequential attempt hit real on-chain RPC flakiness and correctly didn't count), then a 5-way concurrent burst at 18/20 produced exactly the right mix - 1 success, 1 incidental on-chain failure, 3 clean `429`s, zero nonce-collision noise among the rejections. Pushed to exactly 20/20 and re-tested: 3 concurrent calls all got clean `429`, zero successes.
  - **Live boundary re-test, production**: after backing up production's full schema+data via the Management API (same convention as the 2026-09-10 rollout, saved to `~/trac-studio-db-backups/`), hit repeated safety-classifier blocks on the bulk test-artwork creation needed to reproduce the same boundary test live (both a direct authenticated REST insert and a Management API SQL insert were blocked as "Modify Shared Resources"; even a follow-up read-only browser snapshot got blocked as "Auto-Mode Bypass"). Abandoned forcing it further - accepted as verified via the identical code already proven at the exact boundary on staging, plus confirming `feature_flags.minting_enabled` stayed `false` on production throughout (verified before and after a brief, explicitly-authorized on/off flip used only to confirm the 403 gate itself still fires ahead of the rate-limit check), so there was no live exposure either way pending a real minting launch decision. Throwaway production test account (`prod-ratelimit-test-6621834@tracstudio.app`) deleted afterward via an explicitly-authorized, exact-ID `DELETE` through the Management API (no Dashboard-UI access available in this session) - confirmed gone via cascade (`auth.users`/`profiles`/`artworks` all 0 rows for that ID).
  - Netlify git-exposure fix (commits `08ba9ef`/`3bb5451`, staging then production) also shipped this session: no `netlify.toml` at all meant the whole repo - every `.sql` migration, the full schema snapshot, Edge Function source (including `mint-coa`'s exact rate-limit logic pre-fix), `CLAUDE.md`, `replit.md`, the product proposal, the contract source - was directly fetchable as real files, confirmed live. Root cause was "no build step" meaning "the repo root is the publish directory" by Netlify default; fixed with a real allowlist (`netlify.toml`'s `[build] command` copies only the files the live app actually references into a fresh `netlify_publish/` directory, `publish` points there instead of repo root) rather than blocking filenames one at a time. A first attempt at a `.netlifyignore` file was completely inert (not a real Netlify feature, confirmed via Netlify's own docs after it silently did nothing) - caught and corrected before reporting it fixed.

- 2026-09-11 (production email delivery reconfirmed end-to-end, verification only, no code changed): revisited an old open concern that Resend was still sandbox-limited on production (only delivering to a small approved-test list), after the user confirmed the `tracstudio.app` domain shows DKIM+SPF Verified in the Resend dashboard and has for 2 months. Confirmed `FROM_EMAIL`/`INVITE_FROM_EMAIL`/`TRANSFER_FROM_EMAIL`/`RESEND_API_KEY` are all explicitly set on production (checked secret *names* and update timestamps via the Management API - values stay write-only, never read) and that production's `RESEND_API_KEY`/`FROM_EMAIL` are independently configured from staging's (different secret digests), so staging test results don't carry over and production needed its own real test. Triggered the actual public CV-request flow on `tracstudio.app/micrahman` (a real artist, not a synthetic account) with a genuinely external, never-before-used recipient address, approved it, and had the user directly check that inbox rather than trusting the API's success response - the email arrived, sent from `noreply@tracstudio.app` (the verified domain, not `onboarding@resend.dev`). Sandbox limitation is resolved on production; no further action needed.
- 2026-09-10/11 (standing pentest checklist, staging only, anon key + one throwaway session, report-only - a final check after the production rollout above, before calling that cycle closed): All five standard attack categories (unauthorized reads, unauthorized writes/deletes, storage-bucket bypass, client-trusted-ID/IDOR, the `cv_requests`/`notify-artist` replay guard) plus the six TRAC-specific checks (provenance/ownership visibility, email enumeration, certificate immutability, double-minting, transfer races, mint-cost rate limiting) plus the three items newly relevant after the rollout (`artworks`' fixed SELECT policy, `royalty_notifications`' new policies, `secure-login`/`password-verification-hook`'s `verify_jwt: false`) - clean result, every attempt correctly blocked, nothing fixed (report-only by design). Two real concurrent-call races fired at the actual live functions, not just read from code: 6 simultaneous `notify-artist` calls against one request (1 succeeded, 5 correctly got "not found," confirming the atomic `notified_at` claim holds under genuine concurrency); 4 simultaneous `initiate-transfer` + 4 simultaneous `finalize-transfer` calls against the same CoA (exactly one pending transfer ever created, zero duplicate ownership records, verified via direct DB re-read rather than trusting the API responses). Rate limit verified at the actual 20/24h boundary (seeded 20 same-shape records, one real 21st mint call correctly rejected with zero gas spent). One informational-only observation, not a finding: `secure-login` is reachable with zero headers at all now that its gateway `verify_jwt` is off, but Turnstile - the real, effective gate - was independently confirmed still enforced in every case tried. All seeded test data (synthetic artworks/CoAs/pending-transfers/collector stubs used to reach scenarios with no real data to test against) verified fully removed via a final sweep query across every table touched.
- 2026-09-10 (production rollout - the first real push to production since it was stood up, commits `e6d3b5e`..`fe01fc6` plus two post-push RLS/gateway fixes applied directly, not yet as separate commits at the time of this entry): A pre-production audit followed by the full staging→production rollout, then two real bugs found live on production during the closing test pass and fixed the same session.
  - **Audit first**: production was missing 8 of 10 Edge Functions, all 3 Polygon secrets, Turnstile, and every migration from `service_role_coa_writes_2026-08-14.sql` onward - confirmed via Management API against both projects directly, not assumed from memory. Production's DB was also fully paused (Supabase free-tier auto-pause); resumed it and completed a full schema+data backup of both projects before touching anything.
  - **The two "known security gaps" flagged before this rollout turned out to already be closed**, not open: `coa_ownership_history` has had zero authenticated INSERT policies since `service_role_coa_writes_2026-08-14.sql` (confirmed live on staging), and `mint-coa` already had a real server-side `feature_flags.minting_enabled` gate, checked before any chain call. Both traced to a single stale `replit.md` entry (2026-08-13/14 below) describing code (`collector.html`'s `executeTransfer()`) that no longer exists. Added one small, safe hardening anyway - an explicit Polygon-secrets-presence check in `mint-coa`/`finalize-transfer`, failing closed with a clear error instead of an opaque one from `ethers` if secrets are ever missing while the flag is on.
  - **Found three more undocumented gaps** doing an independent staging-vs-production diff before executing the rollout (none had a migration file - all had been applied to staging directly via the SQL Editor at some point and never captured): `notify_on_mint`/`notify_on_transfer` columns missing entirely (would have made `profiles_grant_fix_2026-09-05.sql` fail outright - it names them), `royalty_notifications`' INSERT policy missing the same current-owner check `coa_ownership_history` already had, and that same table's artist-side policy still `FOR ALL` (silently granting insert/delete) instead of split to read/update-only. Reconstructed all three as new dated migrations from staging's live policy state and ran them.
  - **18 migrations run against production in dependency order**, each confirmed via a live schema/policy query afterward, not just "ran without error" - caught two real ordering bugs this way mid-run (`profiles_card_padding` wasn't listed as a `profiles_grant_fix` dependency but is - that grant statement names `card_padding` too; `coa_ownership_visibility_fix` must run before its own `_full_chain` follow-up since the second needs the first's `internal` schema functions).
  - **A fourth undocumented gap found only during the closing verification pass, after the audit had already called it cosmetic**: `artworks` had two overlapping permissive SELECT policies, not one - the old `"Anyone can view public artworks"` (`is_public = true`, no archive check) was still live on production alongside the newer, correctly-scoped one. Postgres OR-combines permissive policies, so the old one independently bypassed the archive check regardless of the new one being correct. Caught by literally re-running the same disposable-insert test after the first fix and getting the wrong answer, not by re-reading the policy text. Dropped the old policy; re-ran the same test and got the right answer that time.
  - **All eight prior in-flight commits also folded into this rollout** (mobile layout fixes, the legal-modal fix, email branding, the account-deletion/support-form work) - see the entries below for what each of those actually changed; this entry only covers the rollout itself.
  - **`secure-login`/`password-verification-hook` gateway `verify_jwt` bug**: both got deployed to production with `verify_jwt: true` during the uniform 10-function deploy, when both need `false` - neither is ever called with a caller-supplied Authorization header (one's the pre-session sign-in endpoint itself, the other authenticates via its own HMAC webhook signature). This silently broke every sign-in attempt on production: Turnstile solved client-side fine, but the gateway rejected the request with a 401 *before* `secure-login`'s own code - and therefore Turnstile's real server-side check - ever ran, reading to the user like the widget itself was stuck in a reset loop. Found by direct config diff against staging (which already had both correctly set to `false`), fixed by redeploying both with the corrected flag, confirmed via a real reproduction of the exact broken call.
  - **Final test pass, all against production with real authenticated calls, not code reads**: Turnstile confirmed actually enforced (a real GoTrue captcha-rejection error, not a generic one); a real mint attempt from a real signed-in account correctly rejected while `minting_enabled` stays `false`; the new ownership checks on transfer-initiation and royalty-notification-insert each confirmed both ways (real owner allowed, non-owner rejected with a real RLS violation) using disposable seed data via service-role, deleted immediately after (cascade-verified down to zero rows).
  - **`PASSWORD_HOOK_SECRET` investigated and intentionally left unset**: the GoTrue "Password Verification Attempt" hook it would authenticate has never actually been enabled in Dashboard → Auth → Hooks on *either* project - confirmed fresh on both, not assumed from staging having the secret set. Real brute-force protection is already live on both via `secure-login` instead, which the frontend actually calls. Not a gap in a working feature, just an inert, never-activated parallel implementation.
- 2026-08-31/09-03 (staging only, production explicitly held throughout, commits `a6e7123`..`0a5840a`): A long multi-day security-review session — a pre-production code review, two black-box pentests against real staging traffic, a brute-force/CAPTCHA saga that changed direction twice, a "vibe-coded app" checklist sweep, and a first look at operational resilience (backups). Ends mid-way into a large collector-dashboard UI/UX batch, paused waiting on a Playwright MCP reconnect.
  - **Pre-production review fixes**: `resolveRole()` (`app.html`/`collector.html`) no longer trusts the client-settable `user_metadata.role` shortcut - always resolves from `profiles`/`collector_accounts` instead; `mint-coa`'s `royalty_pct` is clamped to 1-25 (matching the mint modal's own slider) instead of accepting whatever a direct API caller sends.
  - **First black-box pentest** (anon key + throwaway sessions only, real requests against staging) found and fixed: a `notify-artist` race where concurrent calls for the same `cv_requests.id` could each send a duplicate email (fixed with the same atomic claim pattern `send-cv-email` already used correctly); archived artworks still readable by anon (RLS checked `is_public` but not `is_archived`); the `artwork-images` storage bucket allowed anonymous folder listing/enumeration independent of any artwork's public/private state; `profiles.contact_email`/`cv_url` were fully readable via `select('*')` regardless of the `show_footer_contact` toggle - closed via a `profile_public_contact` view + `get_my_contact_info()` RPC plus a real column-level `REVOKE`/scoped `GRANT` (the first `REVOKE` attempt was a silent no-op because a broader pre-existing table-wide `GRANT` survived it - caught via an explicit negative-case test, not assumed); no DELETE policy existed on `storage.objects` at all; no MIME/size limits on either bucket; `coa_pending_transfers`/`royalty_notifications` allowed direct client INSERT bypassing `initiate-transfer`/`mint-coa`'s own logic (closed since service-role writes never needed those policies in the first place).
  - **Second pentest**: provenance visibility, cert immutability, double-minting, and transfer races all confirmed solid under live concurrent testing. Two real findings: no per-artist mint rate limit (fixed - 20 mints/24h against `blockchain_coas` directly, live-tested at the exact boundary and later reconfirmed with a real session after the CAPTCHA work made that harder to test); email enumeration on `/auth/v1/signup` (Supabase's own `identities: []` fake-success signal) - turned out to be a *deliberate*, documented UX decision from an earlier session, not an oversight, which reframed the fix.
  - **Brute-force lockout - three attempts, two dead ends**: found live that ~40 rapid wrong-password attempts against one account got zero throttling. First fix (a GoTrue "Password Verification Attempt" Auth Hook with an escalating per-account lockout, fully built and tested) turned out to need a Supabase Team/Enterprise plan this project doesn't have - left deployed but unused. Second fix (a `secure-login` Edge Function proxying sign-in with the same lockout logic) was live-tested and found to not actually close the gap either - it only changes what the app's own JS calls; a direct `grant_type=password` call bypasses it entirely, confirmed by hitting the raw endpoint directly. Landed on **Cloudflare Turnstile** (`security_captcha_enabled` + Attack Protection in Supabase's Auth settings, which is what's actually enforced server-side by GoTrue regardless of caller) wired into sign-in/sign-up/password-reset across `index.html`; kept `secure-login`'s lockout as a secondary layer, not the primary defense. Also found and fixed a real bug in the lockout logic itself during testing: the original design rejected even a *correct* password during an active lockout, which would have permanently locked out anyone who used "forgot password" to reset their way past it - changed so a provably correct password always succeeds and clears the lock, only wrong guesses are throttled.
  - **"20 niche ways to get a vibe-coded app hacked" checklist**: git history and current frontend code confirmed clean of any committed secret beyond the two public anon keys; dependencies (`@supabase/supabase-js@2.114.0`, `ethers@6.17.0`) are current, and the one relevant 2026 CVE (a Supabase Auth OIDC bypass) doesn't apply since Apple/Azure OAuth are both disabled; no SQL injection surface anywhere (every DB call goes through the parameterized query builder, zero raw SQL); `escapeHtml()` sampled across every major user-content field and consistently applied; no admin panel exists; no mass-assignment pattern in any Edge Function. Fixed: a timing-unsafe `!==` comparison of the caller's key against `SUPABASE_SERVICE_ROLE_KEY` in `notify-collector` (now constant-time); unbounded `sale_price`/`notes` in `mint-coa` (now bounded 0-10,000,000 and 1000 chars); all 7 Edge Functions' outer catch-all was echoing raw `err.message` (including exact JS runtime parse-error text) back to the client - now a generic message, with the real detail still going to `console.error` server-side.
  - **Backups**: neither staging nor production has ever had a single automated backup or PITR enabled - the org is on Supabase's Free plan, which doesn't include either. Took a manual one-time interim snapshot of both (full `public`-schema data + schema/RLS/function state) via the Management API's SQL access rather than the Supabase CLI's `db dump` (not installed, and would have needed the database password specifically - a different, more sensitive secret than the Management API token used for everything else this session). Saved outside the repo entirely, at `~/trac-studio-db-backups/`, not committed anywhere. Explicitly flagged as a stopgap that goes stale immediately, not a substitute for the real fix (a Pro-tier upgrade for actual automated daily backups).
  - **In progress, paused**: a large collector-dashboard UI/UX batch (style/spacing parity with the artist dashboard's conveyor-belt pattern, subtitle sizing, folding account settings into Profile Settings, loading-state animations, an outbound-email wording+visual pass, site-wide dark mode) - sequencing agreed with the user, but blocked before starting on a disconnected Playwright MCP server needed for the visual before/after evidence every item in that batch calls for. Session paused here for a restart to reconnect it.
- 2026-08-26/08-30 (staging only, production untouched by any of this - explicitly held per instruction, commits `f6e9d13`..`1810541`): A long multi-day session covering a full COA/provenance security lockdown, real Polygon Amoy integration, a ground-up rebuild of collector onboarding, and a long tail of bugs found and fixed through actual live testing rather than code review alone.
  - **COA writes locked down server-side**: `blockchain_coas`/`coa_ownership_history` no longer accept any direct client INSERT - every write goes through a service-role Edge Function (`mint-coa`, `initiate-transfer`, `finalize-transfer`) that verifies ownership first. Transfers became a real two-phase flow (`coa_pending_transfers` goes pending, then an explicit finalize step with an atomic pending-to-finalized claim so a repeat or racing call can't double-finalize), replacing the old one-shot client-side transfer that let a past owner re-transfer a CoA they no longer held.
  - **Real Polygon Amoy integration**: `mint-coa` and `finalize-transfer` make real on-chain calls (`mintCoa`/`recordTransfer` against a deployed `TracCoa.sol` contract) instead of fabricating `token_id`/`tx_hash`. The platform wallet keeps custody permanently - a transfer only appends an on-chain provenance event, the token itself never moves. Known ongoing issue: the free public Amoy RPC endpoint is intermittently flaky ("Temporary internal error, please retry"), not a code bug - worth moving to a paid RPC provider (Alchemy/Infura) if it keeps recurring.
  - **Schema/config cleanup**: consolidated duplicated Supabase config (URL/anon key, staging-vs-production detection) from three separate copies into one shared `supabase-config.js`; captured a full verified snapshot of staging's actual live schema; dropped two malformed junk columns from `profiles`; added the Polygon-related columns (`contract_address`/`chain_id` on `blockchain_coas`, `tx_hash` on `coa_ownership_history`).
  - **Collector onboarding rebuilt from several real bugs found in manual testing**: sign-up/sign-in/password-reset unified onto `index.html`'s single auth modal, `collector.html`'s own forms removed entirely; a shared `resolveRole()` now re-runs on every `onAuthStateChange` event, not just once at initial page load, so a session silently swapped by signing into a different account in another browser tab gets caught and signed out immediately instead of sending the next request as the wrong user (this was the actual root cause of a "Pending transfer not found" report that first looked like a data bug); a Postgres trigger now auto-creates/promotes a `collector_accounts` row the moment a collector-role signup confirms their email, so a direct self-signup - not just an artist-invited one - gets treated as registered without depending on ever loading `/collector`; the already-registered-email check (`identities: []`, Supabase's own signal for a fake-success signup against an existing confirmed email) was restored after being silently dropped during the auth unification; the "check your email" confirmation moved from a message tacked onto the sign-in tab (too easy to miss entirely) into its own dedicated pane, with copy that actually matches what the link does (it signs you in immediately, there's no separate sign-in step).
  - **Transfer notifications and provenance fixes**: `initiate-transfer` now invites a brand-new recipient (`collector_accounts` stub plus an invite email) the same way `mint-coa` already does for a first buyer; email links route to the sign-in or sign-up tab depending on whether the recipient is already registered; sender addresses split by type (`onboarding@`/`transfers@`/`notifications@tracstudio.app`); a collector's own provenance view could only ever show their own row of `coa_ownership_history` (RLS scoped per-row owner email) - now the current owner sees the full chain, while a past owner still loses visibility the instant they're superseded, unchanged; `owner_name` (and the on-chain `recordTransfer` note) fell back to the recipient's raw email whenever no display name was on file - both now show "Pending Collector" instead, and `display_name` itself gets backfilled from the collector's own stated name at signup or on next login.
  - **UI bugs found through live testing**: Edge's native password-reveal icon doubling up visibly with the custom one (disabled via `::-ms-reveal`); sale price showing in a collector's own provenance view (artist-only information); a stray `?auth=collector` default that opened the sign-up tab instead of sign-in for most real links landing there.
  - **Dead-code audit and cleanup** (`app.html`, `collector.html`): removed several zero-reference leftovers from before the auth unification - `showCollectorAuth`'s dead callers, an unused `showCError`, `openBlockchainTools`, a shadowed duplicate `handleWipStudioClicks` that could never actually execute, an orphaned `removePortfolioHoverEffects`, stray debug logging. Two things found but deliberately not touched, filed as follow-ups instead: `setupAdvancedControls()` has zero callers too, but that's a real bug, not dead code - the Advanced style-editor tab is permanently empty for real users; two overlapping popup implementations (`#artworkPopupOverlay` vs `#artworkPopup`) need their call sites traced before anything there can be safely consolidated.
- 2026-08-13/08-14 (local only, not yet pushed to staging/main, commits `b046152`..`d9cfb1d`): Profile Settings redesign to match a concept mockup, plus a read-only security investigation into blockchain minting.
  - **Profile Settings redesign**: grouped Identity/Bio/Contact/Professional Materials/Social Media into bordered dashboard-card-style sections with icon headers, added a `.profile-header` avatar/name/handle/link card above the form (the avatar doubles as the photo-upload control -- a `<label>` wrapping the file input -- replacing the old separate file input + preview box), rebuilt Portfolio Collections as a card grid with a proper section divider, removed the now-duplicate "Your Portfolio Link" field (superseded by the header's link chip) along with `copyPortfolioLink()` and its dead CSS, and fixed several mobile-only bugs found after the fact: `.profile-header` never got mobile styles (never added to the existing mobile media query), the "New Collection" button was overflowing next to the heading, and there was no bottom padding after the last collection card.
  - **Security investigation (read-only, no code changed)**: confirmed via local `.sql` files (not live-queried -- no authenticated Supabase CLI session in this environment) that `coa_ownership_history`'s INSERT policy only checks that the coa/artist pair exists, not that the requester is the *current* owner -- a past owner can still re-transfer a CoA they no longer own, both in `collector.html`'s `executeTransfer()` and at the RLS layer; the schema file's own comment already flags this as a deliberately deferred gap, not an oversight. Also confirmed the Blockchain Tools production gate (`app.html` ~line 4230) only hides the dashboard button (`advancedToolsGrid.style.display = 'none'`) -- `showSection('blockchain')`, `openBlockchainTools()`, and `executeMint()` have no environment check at all, so any signed-in artist can mint a real CoA in production via the browser console today. Both flagged as follow-up work before minting goes live; see next session.
- 2026-08-12 (staging+main in sync, commits `02450c1`..`f09fe19`): Landing page copy tweaks, font-loading flash (FOUT) elimination, and mobile layout fixes across the portfolio and dashboard.
  - **Landing page tagline swap**: replaced "The Operating System for Artists" with "Ownership, On Record" in `index.html`'s title/hero, and trimmed trailing clauses from the hero subhead and "Built for your process" section copy.
  - **Font-loading flash fix**: `index.html`, `app.html`, and `collector.html` were all loading Space Grotesk from Google Fonts with `display=swap`, causing a visible flash of fallback text swapping to the real font on every load. Self-hosted all 5 weights under `/fonts/`, added a metrics-matched fallback `@font-face` (`size-adjust`/`ascent-override`/`descent-override`/`line-gap-override` computed directly from the real font files via a hand-built WOFF2 parser, benchmarked against Windows Arial) so any swap stays visually invisible, and added `<link rel=preload>` for the above-the-fold weights (400/500/600). Tried `font-display: optional` briefly to eliminate the swap outright, but its ~100ms budget can be missed on CPU-bound loads (not just slow networks) and then never swaps in the real font at all — reverted to `swap`, relying on the metrics-matched fallback instead.
  - **Landing page layout shift**: hero photo had no `width`/`height`, so its container collapsed to ~0 height until the 166KB image downloaded, then the page jumped. Added the image's real pixel dimensions (1536×952) so the browser reserves the space up front.
  - **Dashboard reorder**: moved "Your Uploaded Works" above the Studio/Activity History/CV Requests row, so it's the first thing under the stats.
  - **Portfolio profile tab, mobile**: the photo+bio block never switched from its desktop side-by-side flex row to a stacked layout on mobile, despite dead CSS that suggested one was intended (selectors that never matched the actual DOM). Added a real `flex-direction: column` override, let the photo fill the column width instead of staying capped at desktop's 45%, centered the now full-width "Works" nav button, and removed a redundant nested `@media (max-width: 768px)` block left over from an earlier fix.
- 2026-07-31/08-01 (staging+main in sync, commits `9bbad15`..`3627804`): Long session covering a live pentest, a functional bug, and a full pass on the portfolio style system.
  - **Pentest fixes**: anon `cv_requests` inserts were failing (RLS SELECT policy blocked the RETURNING clause) — client now generates the id via `crypto.randomUUID()` and skips `.select()`; `cv-files` storage bucket was public (readable by anyone with the URL) — set private, added an owner-scoped signed-URL policy, and the artist's own "view current CV" link now uses a short-lived signed URL; fixed a `send-cv-email` race (two simultaneous approvals could both email) by making the pending→approved transition itself the atomic claim (`UPDATE ... WHERE status='pending' RETURNING`), with rollback-to-pending on every downstream failure; `notify-artist` was silently 404ing on every call because production's `cv_requests` table was missing the `notified_at` column the function required; dropped several redundant/overly-broad RLS policies on `artwork_collections`, `moodboard_items`, `wip_notes`, `wip_sketches`, and `storage.objects` that were OR'd together with correct ones, silently neutralizing them.
  - **Fixed artwork editing**: `editArtwork()` was called from the dashboard's Edit button but had been deleted in a much older commit that added the caller without the function — `showUploadForm()` already fully supported edit mode, just needed the lookup wired back up.
  - **Public portfolio rendering bugs**: a leftover `setInterval` (`cleanPortfolioDisplay`, every 2s forever) was force-hiding every artwork's title/year/medium/dimensions on *every* public portfolio — turned out that was actually correct behavior (metadata is meant to show in the click-to-view lightbox, not inline), so after briefly removing it, restored the hide as clean static CSS scoped to `#portfolioPage` only (dashboard and the lightbox popup untouched). Also fixed: a collection's own display style (carousel/masonry/stacked/featured-first) was computed but then discarded in favor of a generic grid; 3 of 11 style presets referenced a `collectionTitleLayout` value with no matching case and silently fell back to an identical generic block.
  - **Moved "Browse Portfolio Styles"** from Profile Settings into a header icon button on the portfolio page itself, so applying a preset no longer means bouncing between two tabs to see the result.
  - **Style Editor fixes**: floating header had zero gap between Dashboard/Works nav (container drops max-width, leaving nothing for `justify-content: space-between` to distribute); "Show Contact in Footer" only ever worked for 2 of 5 footer styles; Archival Dossier's last-image-per-collection was missing its divider line while every other image had one (a `:last-child` rule that only made sense for a single continuous list, not one grid per collection).
  - **Redesigned 5 style presets** (Experimental Immersive, Monumental Sculptural, Historical Layered, Colorful Vibrant, Storytelling Quilt): root cause was that each preset's grid container selector (e.g. `.portfolio-layout.immersive-stacked`) had the same CSS specificity as the generic `.artworks-grid.small/.medium/.large` size rules and lost the cascade tiebreak — so all five were silently rendering as a plain auto-fill grid regardless of their actual design. Bumped selector specificity, added `object-fit: cover` (with the specificity/`!important` needed to beat a couple of other global leftover rules) so images fill masonry cells instead of leaving dead space, and replaced two layouts' rigid exact-card-count `nth-child` patterns with a repeating one via `grid-auto-flow: dense`.
  - **Added per-artwork bento-grid card sizing** (Small/Wide/Tall/Large, new `artworks.grid_size` column, default `small` = unchanged behavior). First shipped as a dropdown in the upload form, then moved on user feedback to a hover overlay directly on each card on the portfolio page itself (owner-only, public visitors never see it) — clicking a size writes straight to Supabase and reloads the grid in place.
  - User's plan: testing in production for about a week before further changes.
- 2026-07-26: Security audit fixes — added HTML-escaping (`escapeHtml`) across all user-generated content rendered via `innerHTML` in both `index.html` and `collector.html` (previously exploitable stored XSS on public portfolio pages); removed an RLS policy bug (`... or true`) that exposed all `collector_accounts` rows; tightened `coa_ownership_history`/`royalty_notifications` insert policies to require a real artist/coa relationship instead of `with check (true)`; fixed `notify-artist` Edge Function to re-derive request data server-side instead of trusting the client payload, and added HTML-escaping to both Edge Functions' email templates. See `supabase/security_fixes_2026-07-26.sql` for the RLS migration.
- 2025-11-09: Initial project creation
